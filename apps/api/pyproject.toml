[project]
name = "personadb-api"
version = "0.1.0"
description = "Django REST API for PersonaDB."
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.15.20",
    "ty>=0.0.56",
]
